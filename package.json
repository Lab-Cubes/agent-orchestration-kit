{
  "name": "nps-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Agent orchestration kit — NOP reference implementation for multi-agent task dispatch. Part of the NPS (Neural Protocol Suite) ecosystem; protocol SDKs live at labacacia.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
