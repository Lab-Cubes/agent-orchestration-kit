{
  "name": "nps-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Reference implementation and adapter kit for NPS (Neural Protocol Suite).",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
