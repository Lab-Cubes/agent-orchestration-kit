name: Secrets Check

on:
  push:
  pull_request:

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for secrets and personal paths
        run: ./scripts/check-secrets.sh
